{
  "version": 2,
  "builds": [
    {
      "src": "apps/customer/package.json",
      "use": "@vercel/next",
      "config": { "projectDirectory": "apps/customer" }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "apps/customer/$1" }
  ]
}
